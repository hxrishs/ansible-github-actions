---
- name: Upgrade or Install Packages on AWS Instances
  hosts: inventories/production/hosts/web_servers
  become: yes
  tasks:
    - name: Update package cache
      package_facts:
        manager: auto

    - name: Upgrade all packages
      package:
        name: "*"
        state: latest
